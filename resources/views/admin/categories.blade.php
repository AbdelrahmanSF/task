<x-app-layout><div class="max-w-4xl mx-auto p-6"><h1 class="text-3xl font-bold mb-6">Category Management</h1>@if(session('success'))<div class="mb-4 rounded bg-green-100 p-3">{{ session('success') }}</div>@endif<form method="POST" action="{{ route('admin.categories.store') }}" class="flex gap-2 mb-6">@csrf<input name="name" required placeholder="New category" class="flex-1 rounded border-gray-300"><button class="rounded bg-indigo-600 px-4 py-2 text-white">Add</button></form><div class="space-y-3">@foreach($categories as $category)<div class="flex items-center gap-3 rounded border bg-white p-4"><form method="POST" action="{{ route('admin.categories.update',$category) }}" class="flex flex-1 gap-2">@csrf @method('PUT')<input name="name" value="{{ $category->name }}" class="flex-1 rounded border-gray-300"><button class="text-indigo-600">Save</button></form><form method="POST" action="{{ route('admin.categories.destroy',$category) }}">@csrf @method('DELETE')<button class="text-red-600">Delete</button></form></div>@endforeach</div></div></x-app-layout>
