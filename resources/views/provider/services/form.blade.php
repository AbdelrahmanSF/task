<x-app-layout><div class="max-w-3xl mx-auto p-6"><h1 class="text-3xl font-bold mb-6">{{ $service->exists ? 'Edit service' : 'Create service' }}</h1><form method="POST" action="{{ $service->exists ? route('provider.services.update',$service) : route('provider.services.store') }}" class="space-y-4">@csrf @if($service->exists) @method('PUT') @endif<input name="title" value="{{ old('title',$service->title) }}" required placeholder="Title" class="w-full rounded border-gray-300"><textarea name="description" required placeholder="Description" class="w-full rounded border-gray-300">{{ old('description',$service->description) }}</textarea><select name="category_id" required class="w-full rounded border-gray-300">@foreach($categories as $category)<option value="{{ $category->id }}" @selected(old('category_id',$service->category_id)==$category->id)>{{ $category->name }}</option>@endforeach</select><input type="number" step="0.01" min="0" name="price" value="{{ old('price',$service->price) }}" required placeholder="Price" class="w-full rounded border-gray-300"><input type="number" min="1" name="duration_minutes" value="{{ old('duration_minutes',$service->duration_minutes) }}" required placeholder="Duration in minutes" class="w-full rounded border-gray-300"><select name="status" class="w-full rounded border-gray-300"><option value="active" @selected(old('status',$service->status)==='active')>Active</option><option value="inactive" @selected(old('status',$service->status)==='inactive')>Inactive</option></select><button class="rounded bg-indigo-600 px-5 py-2 text-white">Save</button></form></div></x-app-layout>
