<x-app-layout><div class="max-w-6xl mx-auto p-6"><div class="flex justify-between mb-6"><h1 class="text-3xl font-bold">My Services</h1><a href="{{ route('provider.services.create') }}" class="rounded bg-indigo-600 px-4 py-2 text-white">Add service</a></div>@if(session('success'))<div class="mb-4 rounded bg-green-100 p-3">{{ session('success') }}</div>@endif<div class="space-y-3">@forelse($services as $service)<div class="flex justify-between rounded-xl border bg-white p-5"><div><h2 class="font-bold">{{ $service->title }}</h2><p>{{ $service->category->name }} · {{ $service->price }} · {{ $service->status }}</p></div><div class="flex gap-3"><a href="{{ route('provider.services.edit',$service) }}" class="text-indigo-600">Edit</a><form method="POST" action="{{ route('provider.services.destroy',$service) }}">@csrf @method('DELETE')<button class="text-red-600">Delete</button></form></div></div>@empty<p>No services yet.</p>@endforelse</div></div></x-app-layout>
