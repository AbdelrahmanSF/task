<x-app-layout><div class="max-w-6xl mx-auto p-6"><h1 class="text-3xl font-bold mb-6">My Bookings</h1>@if(session('success'))<div class="mb-4 rounded bg-green-100 p-3">{{ session('success') }}</div>@endif<div class="space-y-4">@forelse($bookings as $booking)<div class="rounded-xl border bg-white p-5"><div class="flex justify-between"><h2 class="font-bold">{{ $booking->service->title }}</h2><span class="rounded-full bg-gray-100 px-3 py-1">{{ $booking->status->value }}</span></div><p>Provider: {{ $booking->provider->name }}</p><p>{{ $booking->booking_date->format('Y-m-d') }} at {{ $booking->start_time }}</p><p class="font-semibold">{{ number_format($booking->total_amount,2) }}</p></div>@empty<p>No bookings yet.</p>@endforelse</div></div></x-app-layout>
